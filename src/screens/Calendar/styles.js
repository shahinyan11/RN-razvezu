import {StyleSheet} from 'react-native';

const st = StyleSheet.create({
  whiteContainer: {
    paddingHorizontal: 0,
    overflow: 'hidden',
  },
  calendar: {
    width: '100%',
  },
  button: {
    position: 'absolute',
    alignSelf: 'center',
    bottom: 20,
    width: '85%',
  },
});

export default st;
