import {StyleSheet} from 'react-native';

const st = StyleSheet.create({
  container: {
    minHeight: 16,
    paddingBottom: 5,
  },

  button: {
    width: '80%',
    position: 'absolute',
    alignSelf: 'center',
    bottom: 50,
  },
});

export default st;
